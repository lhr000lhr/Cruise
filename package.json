{
  "name": "mock-server",
  "description": "A mock server for homework",
  "license": "MIT",
  "scripts": {
    "start": "json-server --watch _mock/db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  },
  "devDependencies": {
    "typescript": "^3.2.2",
    "webpack": "^4.28.4",
    "webpack-cli": "^3.2.1"
  }
}
